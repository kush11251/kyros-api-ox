# Kyros API
A high-performance RESTful API for managing and analyzing time series data.
## Running the Application
1. Navigate to the project directory and run `go mod init kyros-api`
2. Run `go mod tidy` to install dependencies
3. Run `go run main.go` to start the server